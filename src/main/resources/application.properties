# Sets management endpoints root path to /api/v1 instead of /actuator
management.endpoints.web.base-path=/api/v1

# Enables H2 web console
spring.h2.console.enabled=true

# Changes default db name, username and password
spring.datasource.url=jdbc:h2:mem:employee_db
spring.datasource.username=employee_db_user
spring.datasource.password=123emp456